import { Box } from "@mui/material";
import React from "react";

const Feed = () => {
    return (
        <Box bgcolor={"lightsalmon"} flex={5} p={2}>
            Feed
        </Box>
    );
};

export default Feed;
